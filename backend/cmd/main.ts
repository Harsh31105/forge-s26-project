import process from "process";
import { config, validateConfig } from "../src/config/config";
import { initApp } from "../src/service/server";

async function main() {
    try {
        // TODO: Write after initApp() in server.ts.
        // validateConfig();
        //
        // const app = await initApp();
        //
        // const shutdown = async (signal: string) => {
        //     console.info(`\nReceived ${signal}. Shutting down forgefully...`);
        // }
        //
        // try {
        //     if (app.server?.close)
        // }
    }
}
